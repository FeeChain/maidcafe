# maidcafe — 产品与商业决策（2026-09-17 定稿）

> 内部计划文档。开源发布时可选择是否公开（透明化商业模式在开源社区通常是加分项）。

## 产品分层

| 版本 | 形态 | 价格 | 说明 |
|---|---|---|---|
| **桌面版** | 开源，本地部署（Ollama 本地 LLM + 本地 TTS） | **免费** | 完整学习功能，一分不少。倡导开源模型 |
| 桌面版 BYOK | 同上，用户自填云端 API key | 免费（用户自付 API 费） | 不拦着想用云端的人；我们零成本零运维 |
| **手机版** | PWA 网页（不做原生 App，绕开商店抽成/审核/备案） | 按生词数付费 | 云端 LLM 生成 + 云端 TTS，付的是"便利费" |
| 视频版（远期） | 付费增值 | 单独按生词数计费 | **永远不含在封顶全开内**（成本高一个量级，定价页第一天写明） |

## 定价

- **预充值词包：¥1 / 100 生词**（用户听得懂"词"，不报 token）
- **累计封顶：充值累计到 ¥10–20 → 文本+语音功能终身全开**
  - 设计逻辑：按量起步无心理负担 → 封顶自动变买断 → 消灭订阅焦虑
  - 精算前提：单人生词用量有天然上限（20-30词/天，多数人半途而废）；
    重度用户在"便宜模型+自托管TTS"成本结构下年成本也只有几块钱
- **进度同步：¥1–2**（免费桌面版已经白学了很多，这个价格没人会嫌贵）
- 成本纪律：**便宜模型（国产 API / 本地）+ 自托管 Kokoro TTS**，这是封顶模式成立的前提

## 增长路径

GitHub 开源（桌面版）→ 名气 → 手机 PWA 一个链接直达（无商店墙）→
桌面校准的词库进度 = 换手机付费同步的天然动机（账号同步是商业模式核心部件，不是附属品）

## 技术与法务

- **协议：AGPL**（核心全开源；闭源 SaaS 套壳会被协议卡住）；云端计费/网关小模块不开源
- 词库/音频（用户自带的 .apkg 及其中有道音频）**不入仓库**；
  v3 起所有音频改由开源 TTS 生成（Kokoro 等），版权问题彻底消失
- 单词不单独朗读、全部放进句子/对话里读 → 规避 TTS 孤立词 heteronym 读音歧义

## 路线图

- [x] v1 校准：听音优先过词，两键节奏（第一下凭耳朵 Space/J，第二下凭眼睛 Space/J，
      沾 J 即生词；K 重听；按考纲梯度 500 词/块；实测手速估剩余时长）
- [ ] v1.5 校准双模式（关键洞察：全扫 8900 词≈11 小时，这就是多邻国用抽样估算的原因）
  - **抽样估算模式（默认）**：每块随机抽 20-30 词测认识率 → 认识率高的整块推定为已知、
    生僻端整块推定为生词 → 只在边界块做全扫。普通用户几十分钟出结果
  - **全扫模式**：头铁用户逐词过（当前实现）
  - 注意：**不做语速调节**——自然语速本身是听力训练的一部分，不为省时牺牲
- [x] v2 学习队列（2026-09-17 实现并验证；DESIGN_BRIEF.md 已交接 design）：
  - 词三态：生词池（校准所得，未开始学）→ 学习中（复习梯子 1/3/7/15/30 天，
    "复习"=完整听完包含该词的对话，隐式信号零操作）→ 已毕业（进已知库，做背景词）
  - 毕业双轨：爬完梯子自动毕业 + 手动"我学会了"（斩词）；反向：手动"还是不熟"打回底层、
    "重新学"拉回毕业词
  - 手动加词三入口：生词本搜索框（ECDICT补全）、校准页J、听力室字幕点任意词→释义卡→加入
  - 每日任务一键：今日到期复习词 + 新词配额(默认5/天可调) → 混合打包 6-8词/段 →
    全部生成**全新对话**（复习=新场景重逢，编码变异优于重复例句——本产品招牌）；
    边听边生成，旧对话存档可自选重听（不计学分）
  - 调度算法：五级梯子（粗信号配简算法；FSRS 留给未来细粒度数据）
  - API: word_srs 表 + /api/word/learned|unfamiliar|readd + /api/wordbook_add + 今日任务端点
  - 排序路线做成三选一：①国内考纲梯度（中考→…→GRE，段内按词频；当前默认）
    ②国际纯词频（BNC/COCA）③过渡混合。三者大体相似，给用户选择权即可
- [ ] v3 对话生成：本地 LLM 按当日到期词生成对话（90–98% 已知词校验循环）+ 双音色 TTS
  - [x] 核心闭环 generate→verify→repair 已跑通（dialogue.py；27B+释义注入+常识规则后质量达标）
  - [x] 女仆咖啡厅角色库 roster.json（5人+隐藏面10%触发）+ scenes.json（30%乱入客人）
  - [x] 人数纪律：2人为主(65%)/3人常态上限(20%)/独白(10%)/4-5人稀有(5%)；有客人时台上≤3
  - [ ] 听力室页面（功能清单已定稿见下，视觉交给 design）：
    播放/暂停+空格、逐轮高亮自动推进、点轮重播、整段循环、纯听⇄字幕切换、
    逐轮揭示、生词红色高亮、点生词弹释义卡（音标+释义+单词发音）、角色配色、
    对话历史列表、生成新对话按钮、删除、生词本页（词→出现过的对话）、
    目标词速览条、已知占比徽章
  - 原则：不做"抓到没抓到"打分（用户自评，不打扰）；不做倍速（自然语速是训练的一部分）
- [ ] v3.5 跨平台（mac 先行 → Win/Linux）：核心已是纯 stdlib 天然跨平台；
      待办 = 默认模型名按平台分支（mlx tag 仅限 Apple Silicon）、venv 路径分支
      （Scripts\\python.exe）、say 降级为 mac-only 备胎（Kokoro 为唯一主路）、
      提醒通知三分支（osascript/toast/notify-send）、setup.py 引导脚本
      （urllib 下载 ECDICT+Kokoro+建venv）、GitHub Actions 三平台 CI 冒烟测试
- [ ] v4 账号+同步 → 手机 PWA + 预充值词包
- [ ] v5 视频版（先看音频版付费数据再投入）
