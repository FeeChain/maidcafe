# maidcafe 产品状态机 v3（唯一标准 · 先改此文档再改代码）

> 核心哲学（2026-09-21 用户定稿）：**开门即学，关门即止。没有"每天/任务"概念。**
> 一次打开 = 现抓一份词，背完就是背完；想多学就再来一份/再打开，
> 不想学根本不会打开软件。整个产品唯一的学习落库动作是**考试通过**
> （SRS 爬梯即实时记录），其余一切现算、零保存。

主页 `/` 是状态机宿主。`/calibrate`（校准工具）与 `/listen`（对话史+生词本）
是工具页，从哪来回哪去。顶栏（logo + 生词本·对话史·⚙设置）常驻。

## 0. 状态与全部跳转

| 从 | 动作/按钮 | 到 |
|---|---|---|
| （打开软件） | 未校准 | S0 |
| （打开软件） | 已校准 · 抓到一份词 | **S2（直落，无中间页）** |
| （打开软件） | 已校准 · 复习和生词池都空 | E 空池卡 |
| S0 新客人 | [测词汇量] → 探针 → [去主页开始学习] | I 开学确认卡 → S2 |
| I 开学确认卡 | 只在测完过来那一次出现（"这一轮 N 个词"+[开始]）；刷新即回 S2 | S2 |
| S2 学习中 | [✍ 考试] | S3 |
| S2 | 点一个**已凑齐的组框**（一份切 4 组×5 词，词一次性摆明；翻过组内全部词才解锁；全翻完出现[🔀 打乱重新分组]） | **S6 听对话**（进入即为该组开火） |
| S6 听对话 | [← 回去学] | S2 |
| S6 | [✍ 直接考试] | S3 |
| S6 | [同 5 词再来一场] / [换下 5 个词来一场] · 对话条目展开/收起/播放 · 点词释义卡 | 原地 |
| S3 考试中 | [先不考了，回去学]（只有考过的销掉，其余原样回池） | S2 |
| S3 | 考完 · 还有挂 | S4 |
| S3 | 考完 · 全过 | S5a |
| S4 小结 | [继续学剩下的]（唯一按钮） | S2 |
| S5a 这一份全拿下 | [☕ 再来一份] | S2（新一份） |
| 任何状态 | **直接关掉**（正常人的收工方式，无需任何按钮） | （无事发生——考过的早已逐词落库） |
| E 空池卡 | [去校准工具攒生词 →] | /calibrate |

**核心模型（用户定稿，2026-09-21 晚）**：把全部单词排成从易到难的一条线，
探针找到你的**边界块**（粒度=100 词的块），然后**从边界出发向外扩张、
直指所有单词**——这就是排序存在的全部意义。
- **已知集 = 边界块之内的全部单词**（推定已知）+ 显式标记认识的词。
  边界内的词**永不作为学习对象**（an/the 们只做对话的背景底料）；
  生成对话的 90%+ 已知占比按这个集合校验
- **一份词的配方**（每次打开现算，零落库）：到期复习全上 +
  新词补满配额——新词 = 亲手标 J 的生词 ∪ 边界外未标记的词（推定生词），
  合并后**严格按难度序取**，学习前沿随考试通过逐份前移
新词**不预注册**——考过才进梯（level 1，明天到期）；没考过的新词什么都不记，
留在生词池，下次自然还在。考过的复习词爬梯后到期日后移，下次自然不在。
所以"同一天再打开"和"十天后再打开"是同一件事：**现抓，就是新的一份**。

## 1. 各状态规格

### S0 新客人（未校准，一生只见一次）
| 项 | 内容 |
|---|---|
| 显示 | 欢迎语 + 一句产品说明 + 插画位 C1 |
| 主按钮 | **[测词汇量（2-3 分钟）]** → `/calibrate?probe=1`（动态折半探针） |
| 回来 | 探针结果卡 [去主页开始学习 →] → 回 `/` → 直落 S2 |

### S2 学习中（默认落点）
| 项 | 内容 |
|---|---|
| 顶行 | "这一份 **X**/总 已拿下 · 在学 N 个" + [✍ 考试（N 词）]。**没有收工按钮**——收工=关掉 |
| 主体 | 翻卡**按组顺序循环**（第1组5个→第2组5个→…→回到第1组；只有点[🔀 打乱重新分组]才重排）：放音 → 词形遮住 → Space/点击翻开 → Space 下一张；K 重听。卡上标"第N组·新词/复习"。无计分、不催。首个音若被浏览器拦自动播放，提示按 K/点喇叭 |
| 组框区 | 一份按学习顺序切 **5 词一组**（四组框摆明）。**组框只显示还没考过的词**——考过的词当场从框里消失（2026-09-22 用户裁决：旧词残留看着像没更新）；组内**剩余词**全部翻过 → "☕ 可开火"；开火词=组内剩余词，**不足 5 个照煮，不硬凑**；组内全部考过 → "✓ 已拿下"完成态（不可点）；灶上煮着 → "☕ 灶上"+工序（可点进 S6 看进度）。[🔀 打乱重新分组] 在**剩余词**全部翻过后出现，只对未考过的词重切（末组可以不足 5 个） |
| 持久化 | **无**。翻到哪、学了多久，一概不记 |

### S6 听对话（会话内子页 · 听是听、学是学）
| 项 | 内容 |
|---|---|
| 词源 | **当前 S2 词池**（在学的全部词）——考完试池更新，下次进来自然是新词，零分支 |
| 进入 | S2 按 [来一场对话]：跳入本页并立刻开始生成"接下来 5 个词"的一场 |
| 生成区 | **进度条**=时间基准估计（本机最近 5 杯完成时长的平均，无历史默认 130 秒；条随真实已用时间走，工序阶段作底线，未出锅封顶 95%）；工序文案照旧：挑豆 → ①磨咖啡豆→②萃取浓缩→③蒸奶打泡→④调整风味（=写稿四稿）→ ⑤拉花（=配音）→ **☕ 客人请用**；文案带**实时秒表**"已 m:ss · 平常约 m:ss"（每秒走字）；12 分钟超时/翻车红字说人话；出锅后 [☕ 同组换个场景，再来一杯]。**煮着可离开**：[← 回去学] 回 S2 继续翻卡，组框显示"☕ 灶上"+工序，出锅 toast；生成中显示**安心提示**"煮着不耽误学——回去照常翻卡，出锅了会提醒你"（2026-09-22 用户："要不然真不敢切"） |
| 对话列表 | 今天的对话**往下逐条追加**（旧上新下），一场=一条，点击展开/收起；新出锅的自动展开 |
| 内嵌播放器 | 展开后就地播放：▶/⏸(Space)、◂上一轮/下一轮▸、逐轮揭示（●●●→文字）、说话人配色+头像位、目标词红标、**点任意词**→释义卡（自动暂停，关闭续播，可加入生词本） |
| 跳出 | [← 回去学] → S2；[✍ 直接考试] → S3 |
| 恢复 | 煮着的对话跨状态/重开监视不断；人在 S2 时按钮显示"灶上煮着…"，出锅 toast |

### S3 考试中（用户主动发起，主动权永远在用户）
| 项 | 内容 |
|---|---|
| 顶行 | "考试 X/N · 过 A · 挂 B" + [先不考了，回去学] |
| 主体 | 两键考卡：放音 → Space听出来了/J没 → 词形释义 → Space会读/J不会；K 重听。**双 Space=过 → 即时落库爬梯**（唯一的持久化动作）；沾 J=挂（无记录零惩罚） |
| 出口 | 考完：有挂 → S4；全过 → S5a。中途退出 → S2（考过的已销掉） |

### S4 考试小结（本轮考完还有挂）
| 项 | 内容 |
|---|---|
| 显示 | "通过 A · 还剩 B 个没拿下" |
| 按钮 | **[继续学剩下的]**（唯一按钮）→ S2。想收工？直接关掉 |

### S5a 这一份全拿下
| 项 | 内容 |
|---|---|
| 显示 | 庆祝 + "N 个词全部通过考试"（N=这一份的**整份**词数，不受打乱重分组剔词影响） + 插画位 C4 |
| 按钮 | **[☕ 再来一份]** → 现抓新一份 → S2（与关掉重开完全等效） |

### E 空池卡（临时占位；正式方案在待定清单）
| 项 | 内容 |
|---|---|
| 显示 | 现状：占位提示。正式方案（后续做）：**恭喜界面**——把已有的都学完了，问用户要不要新增额外单词 → 跳到"添加词库"功能 |
| 原则 | 池空不推送不引导，用户自己知道学完没有；主动拓展是另一回事 |

## 2. 不变量（任何改动不得违反）

1. **开门即学**：已校准用户打开软件的第一屏永远是 S2 翻卡（词池空则 E）。没有菜单页、门厅、待开工页。
2. **关门零成本**：关掉=收工，任何状态直接关，无按钮无仪式无保存——因为除"考试通过"外没有任何值得保存的东西。
3. **过词只有一条路**：考试双 Space → srs_review（同日幂等兜底）。**听完对话不计分**，翻卡不计分，生成对话不预注册新词。
4. **没有"每天"**：一份 = 现抓（到期复习全部 + 新词配额）。同天第 N 次打开照样是完整新一份。不累计、不欠账、不放假。
5. **一次只有一个主行动**；**不催**（不出现任何催考/催学提示）。
6. **挂词零惩罚**：不降级、不记录、不显示挂科历史。
7. **工具页无副作用**：/calibrate、/listen 的操作不产生学习计分。
8. **边界即已知**：已知集=边界块之内全部词+显式认识词；边界内的词永不被
   端上来学；新词供给永远从学习前沿按难度序推进。

## 3. 偏差记录

2026-09-21 v1 发现 3 处偏差已修；v2（门厅方案）被用户否决整版替换为 v3
（开门即学）。v3 落地时同步清理：daily_plan 表废弃、finished/门厅逻辑删除、
/api/listened 不再计分、daily_task 不再预注册新词、历史 level-0 占位行清除。
此后任何流程改动：**先改本文档 → 确认 → 再改代码**。

## 4. 待定

- **E 空池正式方案**：恭喜界面 + "要不要新增额外单词？" → 添加词库功能（词源扩展，另一条产品线）
- S2 翻卡是否要"只转这 5 个"小批次开关（与对话分组联动）
- /listen 对话史页里遗留的「今日任务」按钮与新哲学不符，界面阶段清理
- ~~播放器嵌进主页~~ → 已解决：内嵌进 S6 听对话子页
