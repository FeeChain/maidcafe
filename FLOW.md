# maidcafe 产品状态机（唯一标准 · 先改此文档再改代码）

> 主页 `/` 是状态机宿主：**任何时刻只处于一个状态，每个状态只有一个主行动**。
> `/calibrate`（校准工具）与 `/listen`（对话史+生词本）是**工具页**，不在状态机内，
> 从哪来回哪去。顶栏（logo + 生词本·对话史·⚙设置）在所有状态常驻。

## 0. 状态与全部跳转（一表看完）

| 从 | 动作/按钮 | 到 |
|---|---|---|
| S0 新客人 | [测词汇量] → 探针 → [去主页开始学习] | S1 |
| S1 今日待开工 | [开始今天的学习] | S2 |
| S1（词池空） | [去校准工具攒生词 →]（攒完回来） | S1 |
| S2 学习中 | [✍ 考试] | S3 |
| S2 | [今天到这] | S5b |
| S2 | [来一场对话] / [去听这一场] →/listen→[← 主页] | S2（原样回来） |
| S3 考试中 | [先不考了，回去学] | S2 |
| S3 | 考完 · 还有挂 | S4 |
| S3 | 考完 · 全过 | S5a |
| S4 小结 | [继续学剩下的] | S2 |
| S4 | [今天到这] | S5b |
| S5a 全部拿下 | [☕ 加餐：再来一天的量] | S2 |
| S5b 提前收工 | [再学一会]（清收工标记） | S2 |
| S5a / S5b | 睡一觉，次日打开 | S1 |

**每日份量规则（不累计）**：每天 = 固定一份（默认 20，设置可调）。装盘顺序：
到期复习优先（最早到期在前），剩余名额用新词补满；到期超过 20 的**排明天**，
不叠加、不滚雪球。所以断几天回来任务照样是 20——**不需要"放假"功能**（已砍）。
S5b 收工剩下的词仍挂梯、到期日未动，次日优先装盘。

**判定输入**（每次进主页 boot 读服务端 /api/home_status）：
`calibrated`（标记≥50 或有盲测档案）→ 否=S0；
今日 plan 不存在 → S1；存在且(finished 或 全过) → S5；否则 → S2。

---

## 1. 各状态规格

### S0 新客人（未校准，一生只见一次）
| 项 | 内容 |
|---|---|
| 显示 | 欢迎语 + 一句产品说明（记住你认识哪些词，现做对话给你听）+ 插画位 C1 |
| 主按钮 | **[测词汇量（2-3 分钟）]** → 跳 `/calibrate?probe=1`（动态折半探针） |
| 回来 | 探针结果卡 [去主页开始学习 →] → 回 `/` → 落 S1 |
| 不显示 | 任何其他按钮、选择器、统计 |

### S1 今日待开工（已校准，今天还没建计划）
| 项 | 内容 |
|---|---|
| 显示 | "今天的菜单：复习 **N** · 新词 **M**"（N+M ≤ 每日词量，复习优先新词补满；有排队到期词时小字注明"还有 X 个排在明天"） |
| 主按钮 | **[开始今天的学习]** → 服务端建今日计划（新词挂梯 level0）→ S2 |
| 特例 | N+M=0：菜单行换成"词池空了"，主按钮换 [去校准工具攒生词 →]（跳 /calibrate，禁止建空计划） |
| 次级提示 | 生词池见底时一行小字提示可去校准补 |

### S2 学习中（计划存在 · 未收工 · 还有未通过词）
| 项 | 内容 |
|---|---|
| 顶行 | "今日 **X**/总 已过 · 在学 N 个" + [✍ 考试（N 词）] + [今天到这] |
| 主体 | **翻卡**（随机循环）：放音 → 词形遮住 → Space/点击翻开（词+音标+释义）→ Space 下一张；K 重听；每转完一圈重新洗牌。无标记、无计分、不催 |
| 场景行 | [☕ 给接下来 5 个词来一场对话]（取当前循环位置起 5 词）→ 异步煮，本行显示分步进度 **①写稿(第N/4稿) → ②配音 → ③上桌**（带已用分钟；12分钟超时/报错红字说人话）→ 上桌后出现 [去听这一场 →]（跳 /listen#dialogue=N）+ [换个场景，同5词再来一场]；右侧常驻 [今天听过的对话 →] |
| 跳出 | [✍ 考试] → S3；[今天到这] → 服务端记收工 → S5b |
| 回来 | 刷新/关页重开 → 仍是 S2（计划在服务端）；煮着的对话在**任何状态**都恢复监视，不在 S2 时煮好了弹 toast |
| 听对话往返 | /listen 播放页 [← 主页] 回来落回 S2 原样 |

### S3 考试中（用户主动发起，主动权永远在用户）
| 项 | 内容 |
|---|---|
| 顶行 | "考试 X/N · 过 A · 挂 B" + [先不考了，回去学] |
| 主体 | 两键考卡（与校准同节奏）：放音 → Space听出来了/J没听出来 → 显示词形释义 → Space我会读/J我不会读；K 重听。**双 Space=过**（当场落库：计入今日 done + 爬梯一级）；沾 J=挂（仅本地，不惩罚不落库） |
| 跳出 | [先不考了，回去学] → S2（已过的词保留，挂的和没考到的都回学习池） |
| 出口 | 考完全部：还有挂 → S4；全过（在学=0）→ 自动记收工 → S5a |

### S4 考试小结（本轮考完，还有挂）
| 项 | 内容 |
|---|---|
| 显示 | "通过 A · 还剩 B 个没拿下"（有挂="差一点点☕"，无挂不会到这） |
| 按钮 | **[继续学剩下的]** → S2（学习池=挂的词）；[今天到这] → S5b |

### S5a 今日全部拿下（庆祝态）
| 项 | 内容 |
|---|---|
| 显示 | "今日全部拿下 🎉 · N 个词全部通过考试" + 插画位 C4 + 一行"没考过的词明天会重新端上来"（此态其实没有欠账，文案隐藏） |
| 按钮 | **[☕ 加餐：再来一天的量]** → 服务端往今日计划追加一份新词配额 → S2（这是"多背权利"的唯一入口） |
| 回来 | 刷新 → 仍 S5a（除非加餐后 → S2） |

### S5b 提前收工（点了"今天到这"，还剩 Y 个）
| 项 | 内容 |
|---|---|
| 显示 | "今天到这 ☕ · 已拿下 X/总 · 剩 Y 个明天继续" |
| 按钮 | **[再学一会]** → 清收工标记 → S2（刷新后必须仍是 S2，不许弹回 S5b） |
| 回来 | 刷新 → 仍 S5b |

---

## 2. 不变量（任何改动不得违反）

1. **每天闭环且不累计**：任何一天无论停在哪个状态，次日 boot 必然回 S1；每日任务恒为一份量（复习优先装入、新词补满、超量排明天）。断几天回来任务不变多。
2. **一次只有一个主行动**：每个状态最多一个 mknext 主按钮。
3. **考试主动权在用户 + 不催**：任何状态不出现催考/催学的提示、动画、角标。
4. **过词只有一条路**：考试双 Space。听完对话不计分、翻卡不计分（srs_review 同日幂等是兜底，不是入口）。
5. **刷新无损**：任何状态刷新/关页重开，回到同一状态；煮着的对话跨状态恢复监视。
6. **挂词零惩罚**：考挂只回学习池，不降级、不记录、不显示历史挂科次数。
7. **工具页无副作用**：/calibrate 与 /listen 的任何操作不改变今日状态机所处状态（听完当日新对话会触发同日幂等的爬梯，被 4 兜住）。

## 3. 偏差记录

2026-09-21 首版发现 3 处代码偏差（S5b 再学一会没清收工标记 / S5a 卡片规格 /
空词池可建空计划），已全部修复归零。此后任何流程改动：**先改本文档 → 确认 →
再改代码**，代码与文档不一致即为 bug。

## 4. 待定（流程层面，下次讨论）

- 播放器是否嵌进主页 S2（免去 /listen 往返）——涉及界面，先不动
- S2 翻卡是否要"只转这 5 个"的小批次开关（对话分组与翻卡联动）
- 跨天菜单要不要区分"昨天欠的"和"正常到期的"
